"use client";
import { useEffect, useState } from "react";
import { useDebounce } from "@/hooks/useDebounce";
export function SearchBar({ onSearch }: { onSearch: (value: string) => void }) { const [value, setValue] = useState(""); const debounced = useDebounce(value.trim()); useEffect(() => { onSearch(debounced); }, [debounced, onSearch]); return <label className="flex w-full items-center gap-3 rounded-2xl border border-white/15 bg-black/35 px-4 py-3 text-left backdrop-blur-md focus-within:border-gold/80"><span aria-hidden="true" className="text-zinc-400">⌕</span><span className="sr-only">Search movies</span><input value={value} onChange={event => setValue(event.target.value)} placeholder="Search a title, director, or feeling..." className="min-w-0 flex-1 bg-transparent text-sm text-white outline-none placeholder:text-zinc-500" />{value && <button type="button" onClick={() => setValue("")} className="text-xs text-zinc-400 hover:text-white" aria-label="Clear search">Clear</button>}</label>; }
