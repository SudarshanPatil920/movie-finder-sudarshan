import Image from "next/image";
import Link from "next/link";
import { FavoriteButton } from "@/components/movies/FavoriteButton";
import { imageUrl, releaseYear } from "@/lib/utils";
import type { Movie } from "@/types/movie";
export function MovieCard({ movie }: { movie: Movie }) { return <article className="group"><Link href={`/movie/${movie.id}`} className="block focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-4 focus-visible:outline-gold"><div className="relative aspect-[2/3] overflow-hidden rounded-xl bg-card shadow-lg transition duration-300 group-hover:-translate-y-1 group-hover:shadow-2xl"><Image src={imageUrl(movie.posterPath)} alt={movie.posterPath ? `${movie.title} poster` : "Poster unavailable"} fill sizes="(max-width: 640px) 50vw, (max-width: 1024px) 33vw, 16vw" className="object-cover transition duration-500 hover:scale-105 group-hover:scale-105" /><div className="absolute inset-x-0 bottom-0 h-1/2 bg-gradient-to-t from-black/80 to-transparent" /><div className="absolute right-3 top-3"><FavoriteButton movie={movie} /></div><div className="absolute inset-x-3 bottom-3 flex justify-between text-xs font-medium"><span>{releaseYear(movie.releaseDate)}</span><span className="text-gold">â˜… {movie.rating.toFixed(1)}</span></div></div><h3 className="mt-3 truncate font-medium text-zinc-100">{movie.title}</h3></Link></article>; }
