import { Button } from "@/components/ui/Button";
export function Pagination({ page, totalPages, onPageChange }: { page: number; totalPages: number; onPageChange: (page: number) => void }) { return <nav className="mt-12 flex items-center justify-center gap-4" aria-label="Movie pages"><Button onClick={() => onPageChange(page - 1)} disabled={page <= 1} className="bg-white/10 text-white hover:bg-white/20">Previous</Button><span className="text-sm text-zinc-400">Page {page}{totalPages > 0 ? ` of ${Math.min(totalPages, 500)}` : ""}</span><Button onClick={() => onPageChange(page + 1)} disabled={page >= Math.min(totalPages, 500)}>Next</Button></nav>; }
